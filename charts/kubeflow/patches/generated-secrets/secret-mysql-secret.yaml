# Deleted so bootstrap-secrets.sh owns this Secret instead — see
# ../../kustomization.yaml. One resource per file because kustomize 5.4.2
# segfaults on multi-document `$patch: delete` files.
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
$patch: delete
