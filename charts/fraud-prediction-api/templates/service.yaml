apiVersion: v1
kind: Service
metadata:
  name: fraud-prediction-api
  labels:
    app.kubernetes.io/name: fraud-prediction-api
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: fraud-prediction-api
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
